var shareContainer = $('#shareLocation');
var createContainer = $('#createLocation');

shareContainer.append("<%= j(render 'location/success_from_event.html') %>");
shareContainer.toggleClass('active');
createContainer.toggleClass('active');
selectLocation(<%= @location.id %>, "<%= @location.name %>");

var shareContainer = $('.share-container');
var createContainer = $('.create-container');

shareContainer.html("<%= j(render 'location/success_from_event.html') %>");
shareContainer.toggleClass('active');
createContainer.toggleClass('active');
selectLocation(<%= @location.id %>, "<%= @location.name %>");